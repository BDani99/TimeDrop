{
  "_comment": "Copy to env.json (gitignored) and fill in. RevenueCat → Project Settings → SDK API keys. These are the public appl_/goog_ keys, not the secret ones. Without them the app runs in mock mode and nothing can be purchased.",
  "_usage": "flutter build appbundle --release --dart-define-from-file=env.json",
  "REVENUECAT_API_KEY_ANDROID": "goog_XXXXXXXXXXXXXXXXXXXXXXXXXXXX",
  "REVENUECAT_API_KEY_IOS": "appl_XXXXXXXXXXXXXXXXXXXXXXXXXXXX"
}
