name: timedrop_mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Backend / auth / storage
  supabase_flutter: ^2.8.0

  # State management
  provider: ^6.1.2

  # Monetization (RevenueCat) — falls back to mock mode without an API key
  purchases_flutter: ^8.6.0

  # Location
  geolocator: ^13.0.2
  permission_handler: ^11.3.1

  # Reverse geocoding (lat/lng → city) via the native platform geocoder
  geocoding: ^4.0.0

  # Media capture / playback
  camera: ^0.11.1
  video_player: ^2.9.2

  # Photo attachments (up to 3 per capsule) from the gallery
  image_picker: ^1.1.2

  # Decode / crop / mirror drop photo attachments after camera capture
  image: ^4.5.4

  # Async video compression before upload (keeps Storage cost + memory down)
  video_compress: ^3.1.3

  # Secure local storage (device hash, cached session hints)
  flutter_secure_storage: ^9.2.2

  # E2EE crypto primitives
  cryptography: ^2.7.0

  # Local scheduled notifications (unlock-time reminder — no Firebase needed)
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.1

  # Map (pin drop + blurred radar view) — OpenStreetMap tiles, no API key
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # Account linking (Sprint 3) — built against Supabase linkIdentity(),
  # fails gracefully if OAuth providers aren't configured yet
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4

  # Filesystem (temp decrypted media before playback)
  path_provider: ^2.1.5

  # Native share sheet for the generated capsule link
  share_plus: ^10.1.3

  # Fonts (Playfair Display + Plus Jakarta Sans per design/DESIGN.md)
  google_fonts: ^6.2.1

  # share_id / storage path generation
  uuid: ^4.5.1

  # Open Terms/Privacy links from the Paywall
  url_launcher: ^6.3.0

  # Premium UX animations & effects
  flutter_animate: ^4.5.2
  animated_text_kit: ^4.2.3
  palette_generator: ^0.3.3+4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # App icon + native splash screen generation from source images
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Note: assets/icon/ holds build-time-only source images for
  # flutter_launcher_icons + flutter_native_splash (read by path at generation
  # time), so they are intentionally NOT registered as bundled assets here.
  # Exception: android_icon.png is also used at runtime in PaywallScreen.
  assets:
    - assets/icon/android_icon.png

  # To add runtime assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# App icon generation — run: dart run flutter_launcher_icons
flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  image_path_android: "assets/icon/android_icon.png"
  image_path_ios: "assets/icon/ios_icon.png"
  remove_alpha_ios: true

# Native splash screen generation — run: dart run flutter_native_splash:create
flutter_native_splash:
  # Must match splash PNG edge pixels exactly (avoids 1px cream/white seam)
  color: "#F1E5D8"
  # Portrait PNG, slightly zoomed icon, lossless edges
  image: "assets/icon/splash.png"
  android_12:
    # Mild zoom inside circular safe zone
    image: "assets/icon/splash_android12.png"
    color: "#F1E5D8"
  android: true
  ios: true
